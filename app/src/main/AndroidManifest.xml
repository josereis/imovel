<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.imovel.ufpi.imovel">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.DashboardActivity" />
        <activity
            android:name=".views.ListaAnunciosActivity"
            android:label="@string/title_activity_lista_anuncios"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".views.ExibirAnuncio" />
        <activity
            android:name=".views.AnunciarImovel"
            android:label="@string/title_activity_anunciar_imovel"
            android:theme="@style/AppTheme.NoActionBar" />
<<<<<<< HEAD
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".views.MainActivityMaps"
            android:label="@string/title_activity_main_maps"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".views.MapsFragment"
            android:label="@string/title_activity_maps"></activity>
=======
        <activity android:name=".views.CadastraEnderecoActivity" />
        <activity android:name=".views.ConfirmaAnuncioActivity"></activity>
>>>>>>> 64d3195e1421bce082b3abe108d178ee395c34de
    </application>

</manifest>